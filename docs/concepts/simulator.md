# Simulator Guide

The `Simulator` class manages and runs multi-agent simulations with configurable dynamics, control, and collision avoidance mechanisms. Includes support for agent tracking, control input updates, manual overrides, logging, and visualization.